name: drive_safe_app
description: TriVision Drive Safe frontend
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^10.0.0
  google_maps_flutter: ^2.6.1
  url_launcher: ^6.3.0
  flutter_spinkit: ^5.2.0
  # provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
